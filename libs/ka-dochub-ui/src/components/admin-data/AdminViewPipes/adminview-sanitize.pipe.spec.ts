import { AdminviewSanitizePipe } from './adminview-sanitize.pipe';

describe('AdminviewSanitizePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminviewSanitizePipe();
    expect(pipe).toBeTruthy();
  });
});
